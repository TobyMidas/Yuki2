# Yuki2
Multi-Container App Deployment With Docker and Ansible

# Set Up
- Vagrant up
- Vagrant ssh
- cd vagrant/
- docker-compose up -d

- Check localhost:3000 

# Errors
- I named ubuntu/2204 and 2004 and it didn't work. Used ubuntu/focal64
I believe that was a syntax error

- I gave a wrong name to the docker-compose file, this obviously hindered it from running. Figured i used _ and not -. Poor
